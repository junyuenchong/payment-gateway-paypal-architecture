# Copy to .env:  cp .env.example .env
#
# Must match root .env (NEXT_PUBLIC_*) and backend APP_BASE_URL port.

NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
NEXT_PUBLIC_PAYPAL_SUPPORTED_CURRENCIES=MYR
